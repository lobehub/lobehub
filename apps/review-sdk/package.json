{
  "name": "@lobehub/review-sdk",
  "version": "1.0.0",
  "private": true,
  "description": "Embeddable review toolbar: annotate a delivered product in place and send it back to its agent through a LobeHub acceptance",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "vite build",
    "deploy": "bun scripts/deploy.ts",
    "dev": "vite build --watch",
    "test": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@zumer/snapdom": "^2.24.10"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vite": "8.0.16",
    "vitest": "5.0.0"
  }
}
