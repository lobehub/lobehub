// Inspector components (customized tool call headers)
export { SolverInspectors } from './Inspector';

// Render components (rich result cards)
export { SolverRenders } from './Render';

// Re-export types and manifest for convenience
export { SolverManifest } from '../manifest';
export * from '../types';
