'use client';

import Page from '@/routes/(main)/settings/provider/(list)';

const MobileWorkspaceProviderSetting = () => <Page mobile />;

MobileWorkspaceProviderSetting.displayName = 'MobileWorkspaceProviderSetting';

export default MobileWorkspaceProviderSetting;
