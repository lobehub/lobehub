'use client';

import { memo } from 'react';

export const LayoutSettingsFooterClassName = 'settings-layout-footer';

const Footer = memo(() => {
  // Footer removed for Pictura AI branding
  return null;
});

Footer.displayName = 'SettingFooter';

export default Footer;
